services:
  viora:
    build:
      context: .
      network: host      # RUN steps during build use the host's DNS stack
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
    restart: unless-stopped
